{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Snake (lldb)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/game-snake/game-snake",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "lldb",
            "preLaunchTask": "build snake"
        }
    ]
}
